---
- hosts: all
  become: true
  become_method: runas
  connection: winrm
  vars:
    reboot_after_updates: no  
    become_user: amsible

  tasks:
    - name: Check for available updates (optional)
      win_updates:
        state: search
      register: available_updates

